# Copy this file to .env and fill in real SMTP credentials
# SMTP Configuration (required for real email delivery)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM="FleetFlow <no-reply@fleetflow.com>"
